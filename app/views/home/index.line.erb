<%= raw(
  Kamiflex.json(self) do
    alt_text "hello world!"
    bubble do
      body do
        text "hello!"
        message_button "這是一個 Message Button", "傳送的文字", style: "primary"
      end
    end
  end
)%>